<%= form_with(model: car_suggestion, local: true) do |form| %>
  <% if car_suggestion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(car_suggestion.errors.count, "error") %> prohibited this car_suggestion from being saved:</h2>

      <ul>
      <% car_suggestion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :model %>
    <%= form.text_field :model, id: :car_suggestion_model %>
  </div>

  <div class="field">
    <%= form.label :manufacturer %>
    <%= form.text_field :manufacturer, id: :car_suggestion_manufacturer %>
  </div>

  <div class="field">
    <%= form.label :style %>
    <%= form.text_field :style, id: :car_suggestion_style %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
